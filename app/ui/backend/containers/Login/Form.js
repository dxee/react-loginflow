import styled from 'styled-components';
import theme from 'ui/theme';

const Form = styled.div`
  max-width: 325px;
  width: 100%;
  border: 1px solid ${theme.veryLightGrey};
  border-radius: 3px;
  box-shadow: 0px 1px 3px rgba(0, 0, 0, 0.25);
`;

export default Form;
