import styled from 'styled-components';
import theme from 'ui/theme';

const FormHeanderWrapper = styled.div`
  background-color: ${theme.backgroundColor};
  border-bottom: 1px solid ${theme.veryLightGrey};
  padding: 1em;
`;

export default FormHeanderWrapper;
